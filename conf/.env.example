# Vault connection/auth settings loaded at startup.
VAULT_ADDR="https://vault.dev.int:8200"
VAULT_CACERT="devca.crt"

# Vault Agent token sink file (preferred auth mode).
VAULT_TOKEN_FILE="/var/run/secrets/vault/token"
